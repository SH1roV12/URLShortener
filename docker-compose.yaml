services:
    backend:
        image: yaharuott/url_shortener 
        build: .
        
        env_file:
        - .env
        ports:
          - 8080:8080
        depends_on: 
          - database
        restart: on-failure
    database:
        image: mysql:8.4
        env_file:
          - .env
